{
  "name": "{{projectName}}",
  "version": "1.0.0",
  "type": "module",
  "description": "{{description}}",
  "scripts": {
    "dev": "manifold-studio dev",
    "build": "echo 'Build not needed - CLI handles compilation'",
    "test": "vitest"
  },
  "dependencies": {
    "@manifold-studio/wrapper": "{{wrapperDependency}}",
    "@manifold-studio/configurator": "{{configuratorDependency}}",
    "manifold-3d": "^3.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.2",
    "vitest": "^1.0.0"
  }
}
