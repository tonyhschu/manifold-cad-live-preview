---
type: "always_apply"
---

Read README.md and DEVELOPMENT.md for context at the beginning of a session to get project context.

Use the `browser_console_messages` to access a browser and get console logs when developing code running in the browser.

Use the `get-library-docs` tool to check for docs for `Playwright`
